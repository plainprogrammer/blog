<header class="basis-full">
  <h1 class="font-bold text-4xl py-2">James Thompson &bull; CV</h1>
  <hr class="py-2"/>
</header>
<section id="experience" class="basis-2/3">
  <h2 class="font-bold text-3xl">Work Experience</h2>
  <ol>
  <% @work_tenures.each do |tenure| -%>
    <li class="p-2">
      <h3 class="font-bold"><%= tenure.employer %></h3>
      <%= tenure.from_month %>/<%= tenure.from_year %> to
      <%= tenure.to_present? ? 'Present' : "#{tenure.to_month}/#{tenure.to_year}" %>
    </li>
  <% end -%>
  </ol>
</section>
<section id="education" class="basis-1/3">
  <h2 class="font-bold text-3xl">Education</h2>
  <ol>
    <% @education.each do |education| -%>
      <li class="p-2">
        <h3 class="font-bold"><%= education.institution %></h3>
        <h4 class="italic"><%= education.degree %></h4>
        <%= education.to_month %>/<%= education.to_year %>
      </li>
    <% end -%>
  </ol>
</section>
